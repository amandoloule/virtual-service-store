<!DOCTYPE html>
<html lang="pt-br">

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=0.8, maximum-scale=1">
    <title>LouleSoft</title>
    <link rel="shortcut icon" type="image.png" href="/images/icon.png">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/loulesoft.css">
  </head>

  <body>
	<header>
		<nav class="navbar navbar-expand-lg navbar-dark">
			<div class="container">
				<a id="title" class="navbar-brand" href="/">LouleSoft</a>
				<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
					<div class="navbar-nav">
						<a class="nav-link active" href="/">Home <span class="sr-only">(current)</span></a>
						<a class="nav-link" href="/services">Serviços</a>
						<a class="nav-link" href="/subscribers/new">Inscrever-se</a>
						<a class="nav-link" href="/users/new">Cadastrar-se</a>
						<%- include('services/_servicesModal') %>
						<a class="nav-link" href="/chat">CHAT</a>
					</div>
					<div class="ml-auto">
						<% if (loggedIn) { %>
								<span class="logged-text">Logado como <a href="<%=`/users/${currentUser._id}`%>">
									<%= currentUser.fullName %>
								</a></span>
								<span class="log-out">
									<a class="btn btn-sm btn-warning" href="/users/logout">Logout</a>
								</span>
						<% } else { %>
								<a class="btn btn-sm btn-success" href="/users/login">Login</a>
						<% } %>
					</div>
				</div>
			</div>
		</nav>
	</header>

    <main>
		<div class="container">
			<div>
				<% if (flashMessages) { %>
					<% if (flashMessages.success) { %>
						<div class="alert alert-success">
							<%= flashMessages.success %>
						</div>
					<% } else if (flashMessages.error) { %>
						<div class="alert alert-danger">
							<%= flashMessages.error %>
						</div>
					<% } %> 
				<% } %>
			</div>
			<%- body %>
		</div>
	</main>

	<script src="/js/jquery.min.js"></script>
	<script src="/js/bootstrap.bundle.min.js"></script>
	<script src="/socket.io/socket.io.js"></script>
	<script src="/js/loulesoft.js"></script>
  </body>

</html>
